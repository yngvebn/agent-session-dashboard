services:
  dashboard:
    build:
      context: .
      args:
        GIT_SHA: ${GIT_SHA:-unknown}
    ports:
      - "5900:8080"
    volumes:
      - sessions-data:/data
    environment:
      - DB_PATH=/data/sessions.db
    restart: always

volumes:
  sessions-data:
